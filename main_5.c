// Tableau de pointeurs sur fonctions

type_de_retour (* nom_du_tableau_de_pointeurs_sur_fonctions [ taille_du_tableau ] ) ( liste_des_arguments );

/*
Comme vous pouvez le remarquer,
c'est complètement identique à ce qu'a été présenté avant (sauf les [] pour préciser que c'est un tableau).

Son utilisation est soumise à toutes les règles d'utilisation des tableaux ordinaires que vous connaissez,
à savoir le premier indice est 0 et le dernier est taille - 1,
tous les pointeurs qui seront stockés dedans doivent avoir le même type
(les fonctions qu'on mettra dedans doivent avoir le même prototype, autrement dit, le même type de retour et les mêmes arguments).
*/
